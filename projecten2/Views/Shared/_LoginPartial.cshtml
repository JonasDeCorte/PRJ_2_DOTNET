@using Microsoft.AspNetCore.Identity
@using projecten2.Models.Domain

@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

<ul class="navbar-nav">
    @if (SignInManager.IsSignedIn(User))
    {
 <li class="nav-item  px-2 py-2  font-weight-normal">@ViewBag.Begroeting</li>  
<li class="nav-item">
    <a id="manage" class="nav-link text-dark border rounded" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Account overzicht</a>
</li>
  

            }
            else
            {
               
<li class="nav-item">
    <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Login">Login</a>
</li>}
</ul>
